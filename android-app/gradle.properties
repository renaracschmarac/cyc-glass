# Project-wide Gradle settings.

# Required because play-services-location pulls in AndroidX
# dependencies. Without this, AGP 8.x refuses to build with:
#   "Configuration contains AndroidX dependencies, but the
#    android.useAndroidX property is not enabled."
android.useAndroidX=true
